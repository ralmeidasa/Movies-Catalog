<h1> Cadastro de Gênero </h1>

<div>
    <p class="failed_register"><b><%= flash[:error] if @genre.errors.any? %></b></p>
</div>

<div>  
    <% @genre.errors.each do |error| %>
        <p id="error_explanation"><%= error.full_message %></p>
    <% end %>
</div>

<div class="success_register">
    <p><b><%= flash[:notice] if flash[:notice] %></b></p>
</div>

<%= render 'form', genre: @genre %>

<%= link_to 'Voltar', @backpath %>